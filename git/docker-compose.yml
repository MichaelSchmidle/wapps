version: '3.3'
services:
  gitea:
    image: gitea/gitea
    environment:
    - USER_UID=1000
    - USER_GID=1000
    restart: always
    networks:
    - wapps_frontends
    volumes:
    - wapps-git-data:/data
    labels:
    - traefik.enable=true
    - traefik.frontend.rule=Host:${TRAEFIK_HOST}
    - traefik.frontend.entryPoints=https-mgmt
    - traefik.port=3000
networks:
  wapps_frontends:
    external: true
