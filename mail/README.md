# Mail App

After starting the app with ``sudo docker-compose up -d``, point your browser at ``https://${TRAEFIK_HOST}/?admin`` (according to the host you defined in the ``.env`` file). Then use the following credentials:

* Username: admin
* Password: 12345

Remember to change the credentials right after logging into the app.
