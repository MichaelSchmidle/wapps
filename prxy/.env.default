TRAEFIK_HOST=

# Path to SSL certificate and key files,
# i.e. /etc/letsencrypt/live/$domain/fullchain.pem for the certificate
# and /etc/letsencrypt/live/$domain/privkey.pem for the key file
TRAEFIK_PEM=
TRAEFIK_KEY=

# In case you need to restrict Traefik to only listen to a specific IP
# on your host, replace 0.0.0.0 below with the corresponding IP
TRAEFIK_IP=0.0.0.0

###
# Do not edit the following variables
###
COMPOSE_PROJECT_NAME=wapps_prxy
