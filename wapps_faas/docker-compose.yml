version: '3.3'
services:
  iron-functions:
    image: iron/functions
    volumes:
    - data:/app/data
    - /var/run/docker.sock:/var/run/docker.sock
    networks:
    - wapps_frontends
    labels:
    - traefik.enable=true
    - traefik.frontend.rule=Host:${TRAEFIK_HOST};PathPrefixStrip:/api
    - traefik.port=8080
  iron-functions-ui:
    image: iron/functions-ui
    env_file: .env
    environment:
    - API_URL=https://${TRAEFIK_HOST}/
    networks:
    - wapps_frontends
    labels:
    - treafik.enable=true
    - traefik.frontend.rule=Host:${TRAEFIK_HOST}
    - traefik.port=4000
volumes:
  data:
networks:
  wapps_frontends:
    external: true
